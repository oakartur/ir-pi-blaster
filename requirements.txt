# The tools in this repository only depend on the Python standard library.
# Hardware-specific GPIO libraries such as pigpio and lgpio are installed via
# the operating system packages recommended in the README.
